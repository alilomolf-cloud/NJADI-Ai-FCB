      - name: Build Web Production Bundle
        run: |
          mkdir -p dist
          cp index.html dist/
          cp -r *.tsx dist/ 2>/dev/null || true
          cp -r src dist/ 2>/dev/null || true
          npm run build --if-present
